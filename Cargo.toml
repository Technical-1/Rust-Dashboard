[package]
name = "Rust-Dashboard"
version = "1.0.0"
edition = "2021"

[dependencies]
sysinfo = "0.30"

# We include eframe (which includes egui) with default features
# If you need a different backend (e.g. glow), you can tweak features below.
eframe = { version = "0.21", features = ["wgpu"] }